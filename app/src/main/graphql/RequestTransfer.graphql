mutation RequestTransfer($input: AcceptTransferInput!) {
  acceptTransfer(input: $input) {
    transfer {
      id
      ruleType
      status
      packages {
        id
        packageId
      }
    }
  }
}
